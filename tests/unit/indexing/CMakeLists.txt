# Indexing tests have been moved to service layer tests
# Legacy component-level indexing tests have been replaced with IndexingService tests
# See tests/unit/app/services/indexing_service_test.cpp for modern indexing tests

# This directory is kept for backward compatibility but contains no active tests