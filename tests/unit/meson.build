# Unit tests are registered as isolated Catch2 executables from tests/meson.build.
# Keep this stub so parent Meson files can include tests/unit without rebuilding the
# removed aggregate GTest/Catch2 object library.
yams_unit_objs = []
message('All unit tests have individual Catch2 executables; no aggregate unit objects to build.')
