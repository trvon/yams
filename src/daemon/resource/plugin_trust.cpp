#include <yams/daemon/resource/plugin_trust.h>

// The plugin trust helpers are header-only so they can be shared by daemon,
// CLI, and plugin-management code without introducing extra link dependencies.
