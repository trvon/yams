# IDE native file — use with CLion's Meson "Setup options":
#   --native-file meson/profiles/ide.ini
#
# Prevents ccache from being used as a compiler launcher, which avoids
# CLion's broken compiler-info probe (ccache: invalid option -- +).

[binaries]
c = 'clang'
cpp = 'clang++'